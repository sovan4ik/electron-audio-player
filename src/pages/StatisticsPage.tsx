import { Box, Typography } from "@mui/material";

export default function StatisticsPage() {
  return (
    <Box p={3}>
      <Typography variant="h4" gutterBottom>
        Statistics
      </Typography>
      <Typography variant="body1">Statistics here</Typography>
    </Box>
  );
}
